{
  "$defs": {
    "HybridConclusion": {
      "enum": [
        "confirmed",
        "remediated"
      ],
      "title": "HybridConclusion",
      "type": "string"
    }
  },
  "additionalProperties": false,
  "properties": {
    "required_conclusion": {
      "$ref": "#/$defs/HybridConclusion",
      "default": "remediated"
    },
    "timeout_seconds": {
      "default": 10.0,
      "exclusiveMinimum": 0,
      "maximum": 60,
      "title": "Timeout Seconds",
      "type": "number"
    },
    "max_attempts": {
      "default": 3,
      "maximum": 3,
      "minimum": 3,
      "title": "Max Attempts",
      "type": "integer"
    }
  },
  "title": "HybridReleaseGatePolicy",
  "type": "object"
}

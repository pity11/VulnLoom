{
  "additionalProperties": false,
  "properties": {
    "revocation_id": {
      "pattern": "^[0-9a-f]{64}$",
      "title": "Revocation Id",
      "type": "string"
    },
    "record_id": {
      "pattern": "^[0-9a-f]{64}$",
      "title": "Record Id",
      "type": "string"
    },
    "identity_ref": {
      "pattern": "^[0-9a-f]{64}$",
      "title": "Identity Ref",
      "type": "string"
    },
    "reason_digest": {
      "pattern": "^[0-9a-f]{64}$",
      "title": "Reason Digest",
      "type": "string"
    },
    "revoked_at": {
      "format": "date-time",
      "title": "Revoked At",
      "type": "string"
    },
    "credential_material_accessed": {
      "const": false,
      "default": false,
      "title": "Credential Material Accessed",
      "type": "boolean"
    }
  },
  "required": [
    "revocation_id",
    "record_id",
    "identity_ref",
    "reason_digest",
    "revoked_at"
  ],
  "title": "TestIdentityRevocation",
  "type": "object"
}
